# Agent Notes

- Run `moon fmt` and `moon info` periodically to keep formatting and interface files up to date.
- Use Conventional Commits for commit messages (e.g., `feat: ...`, `fix: ...`, `chore: ...`).
